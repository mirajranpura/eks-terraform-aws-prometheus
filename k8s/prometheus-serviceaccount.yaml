apiVersion: v1
kind: ServiceAccount
metadata:
  name: prometheus-server
  namespace: prometheus
  annotations:
    # Replace with your IAM role ARN from Terraform output: terraform output prometheus_role_arn
    eks.amazonaws.com/role-arn: ${PROMETHEUS_ROLE_ARN}
